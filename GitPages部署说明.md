# GitPages 部署说明

## 目标

把本交付包上传到 Git 仓库，并通过仓库 Pages 链接打开每个已修改系统。

## 推荐操作

1. 新建或进入目标 Git 仓库。
2. 解压交付包，不要把 zip 文件直接上传到仓库。
3. 打开解压后的 `点餐C端开票二期-GitPages交付包-20260917` 文件夹。
4. 将该文件夹里面的全部内容上传到仓库根目录，包括 `.nojekyll`、`index.html`、三个系统文件夹和说明文档。
5. 在仓库设置中开启 Pages。
6. 打开 Pages 根地址，进入 `index.html` 后选择系统。

## 可直接给评审方的链接

开启 Pages 后，将下面的 `<仓库Pages根地址>` 替换为真实地址：

```text
总入口：<仓库Pages根地址>/index.html
供应商后台系统：<仓库Pages根地址>/供应商后台系统/
供应商小程序：<仓库Pages根地址>/供应商小程序/
点餐后台系统：<仓库Pages根地址>/点餐后台系统/
```

## 注意事项

- 如果仓库是私有仓库，外部评审人员需要仓库或 Pages 访问权限。
- 如果希望“所有人都可见”，需要使用公开仓库，或确认企业 Pages 已开放匿名访问。
- 请上传本 `GitPages交付包` 目录内的文件，不要上传旧的本地预览交付包。
- 不要只上传 zip 文件，也不要只上传某一个系统文件夹，否则仓库链接无法打开完整页面。
- 本地测试不要直接双击系统目录里的 `index.html`，请使用 `一键本地预览.cmd` 或上传 Pages 后通过仓库链接访问。
